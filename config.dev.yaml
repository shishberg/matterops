mattermost:
  url: "http://localhost:8065"
  channel: "myteam/town-square"  # "team/channel-name" format (the URL slug, not the display name)

webhook:
  port: 8080

dashboard:
  port: 8081

services_dir: "./services"
